services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: you_know_ball
      POSTGRES_USER: you_know_ball
      POSTGRES_PASSWORD: you_know_ball
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
